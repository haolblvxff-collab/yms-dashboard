# Hermes ↔ Trae 自动化协作桥梁
> 本文件由 Hermes（大脑）生成，Trae（肌肉）读取并执行。

---

# [HERMES_TO_TRAE_COMMAND]
**[TIMESTAMP]:** 2026-05-22T18:10:00+08:00
**[TARGET_FILE]:** scripts/daily_error_analyzer.py
**[YE_CONTEXT]:** 日常工艺报错日志分析 — 产线每日产生包含刻蚀/薄膜/CMP/光刻等多模块报错的日志文件，需自动化提取高频异常以辅助 YE 工程师快速定位问题。
**[ACTION_GOAL]:** 编写一个 Python 脚本 `daily_error_analyzer.py`，读取日常工艺报错日志文件（.log 或 .csv），统计出现频次最高的三种异常类型，并输出格式化报告。
**[EXECUTION_LOGIC]:**
- 步骤 1：创建 `scripts/daily_error_analyzer.py`，含标准 `if __name__ == "__main__"` 入口
- 步骤 2：实现 `read_logs(filepath)` 函数 — 支持 .log（逐行解析，正则匹配异常关键词如 ERROR/WARNING/ALARM/ABORT/FAIL/超出规格）和 .csv（读取 Error_Type 列）
- 步骤 3：实现 `analyze_top_errors(records, top_n=3)` 函数 — 使用 `collections.Counter` 统计频次，返回 (异常类型, 频次, 占比) 的排序列表
- 步骤 4：实现 `generate_report(top_errors, total)` 函数 — 输出如下格式到 stdout：
  ```
  ═══════════════════════════════════════
  📊 日报错分析报告 — {date}
  ═══════════════════════════════════════
  Top 1: {type} — {count}次 ({pct}%)
  Top 2: {type} — {count}次 ({pct}%)
  Top 3: {type} — {count}次 ({pct}%)
  ───────────────────────────────────────
  总记录数: {total} | 异常类型数: {types}
  ```
- 步骤 5：`__main__` 中解析命令行参数 `sys.argv[1]` 作为日志文件路径，调用上述函数链
- 步骤 6：添加 docstring 和使用示例（`python scripts/daily_error_analyzer.py /path/to/error.log`）
- 技术约束：仅使用 Python 标准库（`sys`, `re`, `csv`, `collections`, `datetime`），不引入 pandas/numpy
**[STATUS]:** PENDING
